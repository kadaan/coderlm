## {{file}} (lines {{start_line}}–{{end_line}})

The excerpt below shows lines {{start_line}}–{{end_line}} of `{{file}}` from the indexed snapshot.

```
{{content}}
```

### Recommended Next Steps

#### 1. List Symbols in This File
Load all symbols defined in `{{file}}` to see the full inventory of functions, classes, and types without reading the raw source:
```
{{_cli}} symbols --file {{file}}
```

#### 2. Grep for a Pattern
Search across all indexed files for a pattern found in this excerpt. Use this to locate related code in other files:
```
{{_cli}} grep "PATTERN"
```
